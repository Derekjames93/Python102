# Created a list of numbers
list_numbers = [20,50,62,64,30]
# Used the sort method to sort the numbers from least to greatest
list_numbers.sort()
# [0:] calls the most value to the left and so on
print(list_numbers[0:])